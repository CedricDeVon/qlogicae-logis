name: Quality Assurance
permissions:
  contents: read
  pull-requests: write

on:
    push:
    pull_request:

defaults:
    run:
        shell: bash

jobs:
    v3-e2e-testing-about-command:
      name: V3 | About Command
      runs-on: ubuntu-latest

      defaults:
          run:
              working-directory: ./test/qlogicae_logis/v3/github/workspace/usage

      strategy:
          fail-fast: false
          matrix:
              python-version:
                  - "3.14"
                  - "3.13"
                  - "3.12"

      steps:
          - uses: actions/checkout@v4

          - name: PyPi Wait
            run: sleep 10

          - name: Setup Python
            uses: actions/setup-python@v5
            with:
                python-version: ${{ matrix.python-version }}

          - name: Setup Virtual Environment
            run: python3 -m venv .venv

          - name: Activate Virtual Environment
            run: source .venv/bin/activate

          - name: Install Package
            run: pip install qlogicae-logis

          - name: Run 'demo'
            run: qlogicae-logis about version


    v3-type-check-testing:
        name: V3 | Type Check
        runs-on: ubuntu-latest

        defaults:
            run:
                working-directory: .

        strategy:
            fail-fast: false
            matrix:
                python-version:
                    - "3.14"
                    - "3.13"
                    - "3.12"

        steps:
            - uses: actions/checkout@v4

            - name: Setup Python
              uses: actions/setup-python@v5
              with:
                python-version: ${{ matrix.python-version }}

            - name: Install Hatch
              run: |
                python -m pip install --upgrade pip
                pip install hatch

            - name: Install Dev Dependencies
              run: python3 -m pip install -r requirements.txt

            - name: Create Hatch environment
              run: hatch env create

            - name: Run Scripts        
              run: hatch run github-workflow-test-type-check

    v3-lint-testing:
        name: V3 | Lint
        runs-on: ubuntu-latest

        defaults:
            run:
                working-directory: .

        strategy:
            fail-fast: false
            matrix:
                python-version:
                    - "3.14"
                    - "3.13"
                    - "3.12"

        steps:
            - uses: actions/checkout@v4

            - name: Setup Python
              uses: actions/setup-python@v5
              with:
                python-version: ${{ matrix.python-version }}

            - name: Install Hatch
              run: |
                python -m pip install --upgrade pip
                pip install hatch

            - name: Install Dev Dependencies
              run: python3 -m pip install -r requirements.txt

            - name: Create Hatch environment
              run: hatch env create

            - name: Run Scripts        
              run: hatch run github-workflow-test-lint

    # v3-unit-testing:
    #     name: v3 | Unit Testing
    #     runs-on: ubuntu-latest

    #     defaults:
    #         run:
    #             working-directory: .

    #     strategy:
    #         fail-fast: false
    #         matrix:
    #             python-version:
    #                 - "3.14"
    #                 - "3.13"
    #                 - "3.12"

    #     steps:
    #         - uses: actions/checkout@v4

    #         - name: Setup Python
    #           uses: actions/setup-python@v5
    #           with:
    #             python-version: ${{ matrix.python-version }}

    #         - name: Install Hatch
    #           run: |
    #             python -m pip install --upgrade pip
    #             pip install hatch

    #         - name: Install Dev Dependencies
    #           run: python3 -m pip install -r requirements.txt

    #         - name: Create Hatch environment
    #           run: hatch env create

    #         - name: Run unit tests              
    #           run: hatch run github-workflow-test-unit


    # v3-e2e-testing-about-command:
    #   name: v3 | End-To-End Testing | About Command
    #   runs-on: ubuntu-latest

    #   defaults:
    #       run:
    #           working-directory: ./test/qlogicae_logis/v3/e2e/github/workspace/about

    #   strategy:
    #       fail-fast: false
    #       matrix:
    #           python-version:
    #               - "3.14"
    #               - "3.13"
    #               - "3.12"

    #   steps:
    #       - uses: actions/checkout@v4

    #       - name: PyPi Wait
    #         run: sleep 30

    #       - name: Setup Python
    #         uses: actions/setup-python@v5
    #         with:
    #             python-version: ${{ matrix.python-version }}

    #       - name: Setup Virtual Environment
    #         run: python3 -m venv .venv

    #       - name: Activate Virtual Environment
    #         run: source .venv/bin/activate

    #       - name: Install Package
    #         run: pip install qlogicae-logis

    #       - name: Run 'about me'
    #         run: qlogicae-logis about me

    #       - name: Run 'about version'
    #         run: qlogicae-logis about version


    # v3-e2e-testing-workspace-command:
    #   name: v3 | End-To-End Testing | Workspace Command
    #   runs-on: ubuntu-latest

    #   defaults:
    #       run:
    #           working-directory: ./test/qlogicae_logis/v3/e2e/github/workspace/workspace

    #   strategy:
    #       fail-fast: false
    #       matrix:
    #           python-version:
    #               - "3.14"
    #               - "3.13"
    #               - "3.12"

    #   steps:
    #       - uses: actions/checkout@v4

    #       - name: PyPi Wait
    #         run: sleep 30

    #       - name: Setup Python
    #         uses: actions/setup-python@v5
    #         with:
    #             python-version: ${{ matrix.python-version }}

    #       - name: Setup Virtual Environment
    #         run: python3 -m venv .venv

    #       - name: Activate Virtual Environment
    #         run: source .venv/bin/activate

    #       - name: Install Package
    #         run: pip install qlogicae-logis

    #       - name: Run 'workspace setup'
    #         run: qlogicae-logis workspace setup 

    #       - name: Run 'workspace list exports'
    #         run: qlogicae-logis workspace list exports

    #       - name: Run 'workspace export all'
    #         run: qlogicae-logis workspace export all

    #       - name: Run 'workspace import'
    #         run: qlogicae-logis workspace import 


    # v3-e2e-testing-cache-command:
    #   name: v3 | End-To-End Testing | Debug Command
    #   runs-on: ubuntu-latest

    #   defaults:
    #       run:
    #           working-directory: ./test/qlogicae_logis/v3/e2e/github/workspace/debug

    #   strategy:
    #       fail-fast: false
    #       matrix:
    #           python-version:
    #               - "3.14"
    #               - "3.13"
    #               - "3.12"

    #   steps:
    #       - uses: actions/checkout@v4

    #       - name: PyPi Wait
    #         run: sleep 30

    #       - name: Setup Python
    #         uses: actions/setup-python@v5
    #         with:
    #             python-version: ${{ matrix.python-version }}

    #       - name: Setup Virtual Environment
    #         run: python3 -m venv .venv

    #       - name: Activate Virtual Environment
    #         run: source .venv/bin/activate

    #       - name: Install Package
    #         run: pip install qlogicae-logis

    #       - name: Run 'workspace setup'
    #         run: qlogicae-logis workspace setup 

    #       - name: Run 'debug view value-cache'
    #         run: qlogicae-logis debug view value-cache 


    # v3-e2e-testing-filesystem-command:
    #   name: v3 | End-To-End Testing | Filesystem Command
    #   runs-on: ubuntu-latest

    #   defaults:
    #       run:
    #           working-directory: ./test/qlogicae_logis/v3/e2e/github/workspace/filesystem

    #   strategy:
    #       fail-fast: false
    #       matrix:
    #           python-version:
    #               - "3.14"
    #               - "3.13"
    #               - "3.12"

    #   steps:
    #       - uses: actions/checkout@v4

    #       - name: PyPi Wait
    #         run: sleep 30

    #       - name: Setup Python
    #         uses: actions/setup-python@v5
    #         with:
    #             python-version: ${{ matrix.python-version }}

    #       - name: Setup Virtual Environment
    #         run: python3 -m venv .venv

    #       - name: Activate Virtual Environment
    #         run: source .venv/bin/activate

    #       - name: Install Package
    #         run: pip install qlogicae-logis

    #       - name: Run 'workspace setup'
    #         run: qlogicae-logis workspace setup 

    #       - name: Run 'filesystem clean list included'
    #         run: qlogicae-logis filesystem clean list included

    #       - name: Run 'filesystem clean list excluded'
    #         run: qlogicae-logis filesystem clean list excluded


    # v3-e2e-testing-workflow-command:
    #   name: v3 | End-To-End Testing | Workflow Command
    #   runs-on: ubuntu-latest

    #   defaults:
    #       run:
    #           working-directory: ./test/qlogicae_logis/v3/e2e/github/workspace/workflow

    #   strategy:
    #       fail-fast: false
    #       matrix:
    #           python-version:
    #               - "3.14"
    #               - "3.13"
    #               - "3.12"

    #   steps:
    #       - uses: actions/checkout@v4

    #       - name: PyPi Wait
    #         run: sleep 30

    #       - name: Setup Python
    #         uses: actions/setup-python@v5
    #         with:
    #             python-version: ${{ matrix.python-version }}

    #       - name: Setup Virtual Environment
    #         run: python3 -m venv .venv

    #       - name: Activate Virtual Environment
    #         run: source .venv/bin/activate

    #       - name: Install Package
    #         run: pip install qlogicae-logis

    #       - name: Run 'workspace setup'
    #         run: qlogicae-logis workspace setup 

    #       - name: Run 'workflow list selections'
    #         run: qlogicae-logis workflow list selections 


    # v3-e2e-testing-template-command:
    #   name: v3 | End-To-End Testing | Template Command
    #   runs-on: ubuntu-latest

    #   defaults:
    #       run:
    #           working-directory: ./test/qlogicae_logis/v3/e2e/github/workspace/template

    #   strategy:
    #       fail-fast: false
    #       matrix:
    #           python-version:
    #               - "3.14"
    #               - "3.13"
    #               - "3.12"

    #   steps:
    #       - uses: actions/checkout@v4

    #       - name: PyPi Wait
    #         run: sleep 30

    #       - name: Setup Python
    #         uses: actions/setup-python@v5
    #         with:
    #             python-version: ${{ matrix.python-version }}

    #       - name: Setup Virtual Environment
    #         run: python3 -m venv .venv

    #       - name: Activate Virtual Environment
    #         run: source .venv/bin/activate

    #       - name: Install Package
    #         run: pip install qlogicae-logis

    #       - name: Run 'workspace setup'
    #         run: qlogicae-logis workspace setup 

    #       - name: Run 'template list selections'
    #         run: qlogicae-logis template list selections 
